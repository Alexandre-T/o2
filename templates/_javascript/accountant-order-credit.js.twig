
var updateSpan = function() {
  let ten = $('#accountant_credit_form_ten').val();
  let hundred = $('#accountant_credit_form_hundred').val();
  let fiveHundred = $('#accountant_credit_form_fiveHundred').val();

  {{ include('_javascript/_calcul.js.twig') }}
}

//when document is ready, I actualize.
$(updateSpan);

//After each update...
$('#accountant_credit_form_ten').change(updateSpan);
$('#accountant_credit_form_hundred').change(updateSpan);
$('#accountant_credit_form_fiveHundred').change(updateSpan);

//some useful functions
var toFloat = function (value) {
  return parseFloat(value).toLocaleString(window.document.documentElement.lang);
}
var toCurrency = function (value) {
  return toFloat(value) + ',00 €';
}
